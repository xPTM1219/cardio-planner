import type { OsmUserBlock } from "../../types";
import { type FetchOptions } from "../_osmFetch";
/** DWG only */
export declare function createUserBlock(qs: {
    user: number;
    reason: string;
    period: number;
    needs_view: boolean;
}, options?: FetchOptions): Promise<OsmUserBlock>;
