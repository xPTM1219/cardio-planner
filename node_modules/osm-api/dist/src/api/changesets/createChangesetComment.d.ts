import type { Changeset } from "../../types";
import { type FetchOptions } from "../_osmFetch";
/** Add a comment to a changeset. The changeset must be closed. */
export declare function createChangesetComment(changesetId: number, commentText: string, options?: FetchOptions): Promise<Changeset>;
